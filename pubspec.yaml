name: safescreen
description: SafeScreen – Smart Parental Control
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  installed_apps: ^2.0.0
  fl_chart: ^0.70.0
  shared_preferences: ^2.2.0
  geolocator: ^13.0.0

  # Firebase — replaces sqflite + path + crypto completely
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:       
    sdk: flutter
  
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true